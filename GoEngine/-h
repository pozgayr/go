namespace GoEngine;

public enum Stone
{
	Empty,
	Black,
	White
}

public class Board
{
	public cosnt int Size = 19;
	private Stone[,] board = new Stone[size, size];

	public Stone Get(int x, int y)
	{
		return board[x,y;
	}

	public bool Place(int x, int y, Stone color)
	{
		if (board[x,y] != Stone.Empty) return false;

		grid[x,y] = stone;
		return true;
	}

}
